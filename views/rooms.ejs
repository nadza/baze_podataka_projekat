<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style_home.css">
  <link rel="stylesheet" href="/css/style_rooms.css">
  <title>The Database Hotel</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Elsie&family=Quattrocento+Sans&display=swap" rel="stylesheet">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark shadow-5-strong">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
          aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/home" target="_blank">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/type-of-rooms">Rooms</a>
      </li>
      <li>
        <a class="navbar-brand" href="#">
          <img src="/images/logo.png" alt="Your Logo" id="logo">
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/all-reports" target="_blank">Reports</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Contact</a>
      </li>
    </ul>
  </div>
</nav>

  <div class="p-3">
  <%
    function formatString(inputString) {
      return inputString.charAt(0).toUpperCase() + inputString.slice(1).replace(/_/g, ' ');
    }
      for(let i = 0; i < sobeList.length; i++) {
      if (i % 3 === 0) { %>
        <div class="row pb-3 align-items-center">
      <% } %>
          <div class="col-sm-4">
            <div class="card">
              <img src="data:image/jpeg;base64, <%= sobeList[i].slika.toString('base64') %>" alt="Image">
              <div class="card-body">
                <h5 class="card-title"><%= formatString(sobeList[i].vrsta_sobe); %></h5>
                <a href="/type-of-rooms/<%=sobeList[i].id_vs%>" class="btn btn-primary">See more pictures...</a>
              </div>
            </div>
          </div>
      <% if ((i + 1) % 3 === 0 || i === sobeList.length - 1) { %>
        </div>
      <% } %>
  <% } %>
  </div>

</body>
</html>
